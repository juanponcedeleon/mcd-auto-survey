# Core training / inference stack.
# NOTE: the `tesseract-ocr` system binary must be installed separately
#   macOS:  brew install tesseract
#   ubuntu: sudo apt-get install -y tesseract-ocr
torch==2.2.2
transformers==4.41.2
datasets==2.19.1
seqeval==1.2.2
pytesseract==0.3.10
Pillow==10.3.0
numpy==1.26.4
PyYAML==6.0.1
scikit-learn==1.4.2
tqdm==4.66.4

# Optional inference server (scripts/serve.py)
fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.9

# Dev
black==24.4.2
flake8==7.0.0
